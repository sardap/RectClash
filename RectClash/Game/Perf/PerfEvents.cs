namespace RectClash.Game.Perf
{
    public enum PerfEvents
    {
		SEED_SET,
        TICK_UPDATE,
        UNIT_CREATE_SELECTION,
        FACTION_SELECTION,
        CAMERA_MOVED,
        CAMERA_ZOOMED,
		TURN_CHANGED,
		DRAW_TIME_UPDATE,
		UPDATE_TIME
    }
}